interface:
  display_name: "Eazo Three.js Director"
  short_description: "Turn prompts and references into faithful realtime 3D"
  default_prompt: "Use $eazo-threejs-director to turn my visual prompt into a reference-locked, playable realtime 3D experience and verify the live render."

policy:
  allow_implicit_invocation: true
